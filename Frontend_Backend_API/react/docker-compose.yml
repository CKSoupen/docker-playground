version: "3.9"  # optional since v1.27.0
services:
  ck-frontend:
    build: ./client
    container_name: ck-react
    command: npm start /client
    ports:
      - "8000:8000"
    volumes:
      - ./client:/app/client